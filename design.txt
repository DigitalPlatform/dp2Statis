
2023/9/7

同步信息的第一行显示因为什么开始同步。手动触发，还是被定时任务触发，等等

“实例概况”。增加同步时间显示和编辑修改界面元素。
instance 页面能显示和修改同步时间字符串。


当一个实例的同步时间参数修改后，尽量只重新添加这个实例的任务事件(不必全部实例重新初始化)

安装界面登录的时候连接 dp2003.com/dp2library 服务器；而每个实例界面登录的时候连接实例对应的 dp2library URL

登录界面 实例名 具备 Combobox 下拉列表，列出全部可用的实例名

为每日报表配置添加新的馆代码的时候，要检查馆代码的合法性
(初始化阶段要从 dp2library 服务器获得所有的馆代码)

想办法降低 IInstanceRepository 中每次每个 Instance 对象初始化的时间消耗

212 表的每日报表定义行在插入的时候，就自动给 nameTable 里面设置好内容 class_clc

分馆 dp2library 账户使用 dp2statis 的时候，要注意控制好数据安全，不允许越界统计获得其它分馆的数据